{
  "name": "vendor-manager-web",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first vendor database website with optional Google Drive sync.",
  "scripts": {
    "start": "node dev-server.js",
    "dev": "node dev-server.js",
    "check": "node --check public/app.js && node --check public/service-worker.js && node --check dev-server.js"
  },
  "keywords": [
    "vendor-management",
    "local-first",
    "google-drive",
    "static-site"
  ],
  "license": "UNLICENSED"
}
